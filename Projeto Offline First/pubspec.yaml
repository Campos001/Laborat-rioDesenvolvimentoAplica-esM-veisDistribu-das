name: shopping_list_offline
description: Lista de compras com suporte offline-first
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Persistência local
  sqflite: ^2.3.0
  path: ^1.8.3

  # Conectividade
  connectivity_plus: ^5.0.0

  # HTTP
  http: ^1.1.0

  # Gerenciamento de estado
  provider: ^6.1.0

  # Utilities
  uuid: ^4.0.0
  intl: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

